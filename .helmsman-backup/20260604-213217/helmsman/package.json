{
  "name": "helmsman",
  "version": "1.0.0",
  "private": true,
  "description": "Helmsman agent harness — owned skills + tools (hbrowse browser).",
  "type": "module",
  "bin": {
    "hbrowse": "bin/hbrowse.mjs"
  },
  "scripts": {
    "hbrowse:daemon": "node bin/hbrowse-daemon.mjs",
    "browser:setup": "node -e \"require('child_process').execSync('npx playwright install chromium',{stdio:'inherit'})\""
  },
  "dependencies": {
    "playwright": "^1.59.1"
  }
}
