node_modules/
dist/
.astro/
.env
.env.*

# ByteRover — .brv/context-tree/ contains a nested .git managed by brv vc.
# Without these entries, `git add .` fails with "does not have a commit checked out".
.brv/

# Agent infrastructure (synced from gstack/impeccable installs)
.opencode/
.agents/
.claude/

# Restructure scratch (CSS parity refs — not part of the site)
.restructure-refs/
.gstack/
.gbrain-source
